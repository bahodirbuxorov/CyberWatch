services:
  # ──────────────────────────────────────────
  # CyberWatch UZ Bot
  # ──────────────────────────────────────────
  bot:
    build: .
    container_name: cyberwatch-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs

  # ──────────────────────────────────────────
  # Nitter — self-hosted X (Twitter) frontend
  # (Ixtiyoriy) Agar self-hosted Nitter kerak bo'lsa,
  # quyidagi servislarni uncomment qiling va
  # bot servisiga environment qo'shing:
  #   environment:
  #     - NITTER_SELF_HOSTED_URL=http://nitter:8080
  #   depends_on:
  #     nitter:
  #       condition: service_healthy
  # ──────────────────────────────────────────
  # nitter:
  #   image: zedeus/nitter:latest
  #   container_name: cyberwatch-nitter
  #   restart: unless-stopped
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     - redis
  #   volumes:
  #     - ./nitter.conf:/src/nitter.conf:ro
  #
  # redis:
  #   image: redis:7-alpine
  #   container_name: cyberwatch-redis
  #   restart: unless-stopped
  #   volumes:
  #     - redis_data:/data

# volumes:
#   redis_data:
